services:

  nginx:
    build:
      context: ./nginx
    ports :
      - "443:443"
    volumes:
      - /home/abalasub/data/wordpress:/var/www/html
    depends_on:
      - wordpress
    networks:
      - inception_network
  wordpress:
    build:
      context: ./wordpress
    ports:
      - "9000:9000"
    volumes:
      - /home/abalasub/data/wordpress:/var/www/html
    depends_on:
      - mariadb
    networks:
      - inception_network
  mariadb:
    build:
      context: ./mariadb
    ports:
      - "3306:3306"
    env_file:
      - .env
    volumes:
      - /home/abalasub/data/mysql:/var/lib/mysql
    networks:
      - inception_network

networks:
  inception_network: